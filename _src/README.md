# Bronbestanden

Bouw vanuit deze map: `python3 build_v2.py && python3 build_case.py && python3 finalize.py`.
De scripts schrijven naar de bovenliggende map (de site). `_src` wordt niet naar SiteGround gesynct.
